version: 1.3.5
cache: true

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        default:
          - "openai/gpt-5"
        fetch: true
      titleConvo: true
      titleModel: "openai/gpt-5"
      dropParams:
        - "stop"
      modelDisplayLabel: "OpenRouter"
